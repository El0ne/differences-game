export interface GameConstants {
    countDown: number;
    hint: number;
    difference: number;
}

export const DEFAULT_GAME_CONSTANTS: GameConstants = {
    countDown: 30,
    hint: 5,
    difference: 5,
};
