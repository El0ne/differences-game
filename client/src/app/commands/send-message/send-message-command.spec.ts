import { SendMessageCommand } from './send-message-command';

describe('SendMessageCommand', () => {
    it('should create an instance', () => {
        expect(new SendMessageCommand()).toBeTruthy();
    });
});
