import { CloseInfoModalCommand } from './close-info-modal-command';

describe('CloseInfoModalCommand', () => {
    it('should create an instance', () => {
        expect(new CloseInfoModalCommand()).toBeTruthy();
    });
});
